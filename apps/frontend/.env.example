# ApplyFlow frontend environment example
# Copy to .env.local for local use. Never commit real .env files.

# Public browser configuration only. Do not put secrets in NEXT_PUBLIC_* values.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081
NEXT_PUBLIC_AI_ACTIONS_ENABLED=false
